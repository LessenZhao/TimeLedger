import Foundation

// 008: Whole-page NSTextView reading path removed.
// Use `AnnotatableMarkdownReader` (L1 MarkdownBodyView + L2 block selection).
// Intentionally empty of UI types to prevent re-introduction of overlapping layout.
